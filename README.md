# Career Quest · HackAlem AI / Halyk Bank

Карьерный навигатор с портфолио сотрудника, оценкой HR и заявками на обучение. Стартовая база: 200 сотрудников, 60 навыков, 40 событий и 2 743 записи истории из `case/case_1/career_quest_dataset` (срез 2026-10-01). При обычном запуске используются настоящий core и данные кита.

## Установка и запуск

Python 3.11+; исходная версия проверена на Windows / Python 3.11.9, интеграция демовхода — на Python 3.14 / Streamlit 1.64.

```bash
python -m venv .venv
# Windows: .\.venv\Scripts\Activate.ps1
# macOS/Linux: source .venv/bin/activate
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

Открыть http://localhost:8501. Если активация PowerShell запрещена, используйте `.\.venv\Scripts\python.exe` вместо `python`. Для уже запущенного второго сервера этой рабочей папки используется порт 8502:

```powershell
.\.venv\Scripts\python.exe -m streamlit run app.py --server.port 8502
```

Без API-ключа работают профиль, XP, характеристики, сертификаты, решения HR и детерминированные рекомендации core. Поиск внешних курсов сообщает, что ключ не настроен; вымышленные результаты не подставляются. При запуске приложение автоматически загружает `.env` из корня проекта, рядом с `app.py`. Уже заданные переменные окружения процесса имеют приоритет, в том числе пустые значения.

## Страница входа и демороли

При запуске `app.py` сначала открывается страница Career Quest в светло-зелёном стиле. Для проверки интерфейса вход не требует пароля, регистрации или базы аккаунтов:

- **Сотрудник** — выбрать любой профиль стартового кита или импортированный профиль и нажать «Войти в деморежим →». Откроются его профиль и XP, сертификаты, треки, маршрут, карта, навыки и история. Профиль закрепляется на время этого входа.
- **HR** — открыть заявки и решения, оценку выбранного сотрудника или обзор команды.
- **Администратор** — импортировать проверочные данные или посмотреть состояние компонентов и бюджет AI. Управления реальными аккаунтами в этом режиме нет.

Кнопка **«Выйти / сменить роль»** возвращает на страницу входа. Смена роли в той же вкладке сохраняет импорт и прогресс сценария, но обновляет кеш представлений. В новой вкладке или после полного обновления страницы демовход нужно выполнить снова. Роли выбираются свободно: **это демонстрация навигации, не аутентификация или разграничение доступа**.

Демовход хранит только роль и выбранный профиль в `st.session_state`; `AuthService` не вызывается, файл базы аккаунтов не создаётся. Существующее локальное хранилище новых HR-функций (`.career-quest/growth.sqlite3`) продолжает работать без изменений — сертификаты, решения и учёт расходов AI используют его.

Ранее созданная самостоятельная регистрация с SQLite сохранена отдельно в `auth_app.py`. Она не участвует в демовходе `app.py`; подробности и дальнейшая интеграция аккаунтов описаны в [auth/README.md](auth/README.md).

## Сценарий сотрудника и HR

1. **Войти как HR → Оценка сотрудника**: выбрать человека в боковом меню, задать дату выхода и шесть характеристик от 0 до 5. Сохранить. Для следующего шага нажать «Выйти / сменить роль». Характеристики: стрессоустойчивость, коммуникация, инициативность, аналитика, самоорганизация, командная работа.
2. **Сотрудник → Профиль и опыт**: увидеть лепестковую диаграмму и XP. Пока HR не оценил сотрудника, диаграмма показывает пустую сетку. За каждый полный день с даты выхода начисляется 10 XP; будущая дата даёт 0 XP. Каждые 1 000 XP — следующий игровой уровень. Профиль обновляется раз в минуту; XP вычисляется по текущей дате, отдельный фоновый планировщик не нужен.
3. **Мои сертификаты**: указать пройденный курс, провайдера, ссылку, дату, подтверждение и темы/навыки. Заявка остаётся на рассмотрении; опыт и навыки пока не меняются.
4. **HR → Заявки и решения**: одобрить сертификат или отклонить с причиной. Одобрение даёт 100 XP и выбранный HR прирост +1 для навыков каталога, максимум до уровня 5. Можно подтвердить курс без повышения навыков. Повторное начисление исключено.
5. **Треки и курсы → Подобрать треки и найти курсы**: один явный запрос к GPT. Подтверждённые знания и сертификаты используются для персонального объяснения: углубление специализации при концентрированном опыте, несколько направлений при смешанном. Фактические основания показаны отдельно от текста AI.
6. **Хочу на этот курс**: сотрудник подаёт заявку из найденного плана. HR согласует её или указывает причину отказа, например «Нет бюджета». Сотрудник видит статус и комментарий. Согласование обучения не означает покупку или завершение и не начисляет XP.

Дата выхода, оценки, сертификаты, решения, AI-планы и журнал расходов сохраняются в SQLite: `.career-quest/growth.sqlite3`. Не удаляйте эту БД, если хотите сохранить историю и накопленный учёт расходов. БД и секреты исключены из Git.

## AI, поиск и бюджет

Новый подбор использует **`gpt-4.1-mini-2025-04-14`**, OpenAI Responses, Structured Outputs и один вызов `web_search`. Поиск ограничен сайтами учебных провайдеров: Microsoft Learn, Coursera, edX, Cisco, TryHackMe, Hack The Box, AWS и другие. Курс допускается в ответ только при совпадении URL с источником поиска и разрешённым доменом. Это проверка источника, не независимая оценка качества курса; актуальную стоимость нужно уточнять у провайдера.

Модель получает роль, грейд, цель, навыки, подтверждённые курсы и оценки HR. Имя, employee_id, дата выхода и поле подтверждения сертификата не отправляются. Свободный текст названий/тем включается в запрос — не помещайте туда секреты. Поисковый запрос должен содержать учебную тему, без персональных данных.

### Ограничение расходов

- Общий лимит приложения: **$5**; на запрос: **$0,10**. Переменные окружения могут только уменьшить эти значения.
- До сети проверяется верхняя оценка токенов и поиска; в транзакции SQLite резервируется полный лимит запроса. Одновременные запросы используют общий журнал расходов.
- Один поиск, максимум 3 000 выходных токенов, без автоматических повторов. Просмотр вкладок и переключение сотрудников не запускают платные запросы.
- План кешируется на 7 дней. Изменение подтверждённого портфолио сбрасывает применимость плана; устаревший план нельзя использовать для новой заявки.
- После ответа используется консервативный расчёт по usage. При таймауте или неизвестной стоимости весь резерв учитывается как потраченный. После сбоя процесса незавершённый резерв сохраняется.
- Ожидание ограничено 28 секундами. При сбое доступны объяснения и рекомендации исходного движка во вкладке «Мой маршрут».

Тарифы, проверенные 2026-09-23: GPT-4.1 mini — $0,40 за миллион входных и $1,60 за миллион выходных токенов; поиск — $0,01 плюс блок 8 000 входных токенов. Для ограничения приняты цены без скидки кеширования. Источники: [модель](https://developers.openai.com/api/docs/models/gpt-4.1-mini), [поиск](https://developers.openai.com/api/docs/guides/tools-web-search), [тарифы](https://developers.openai.com/api/docs/pricing).

Журнал — консервативная оценка приложения, не баланс аккаунта OpenAI. Вызовы других приложений и экземпляров с отдельной БД не входят в этот лимит. При изменении тарифов провайдера расчёт нужно обновить. При восстановлении БД из старой копии восстанавливается и старый журнал расходов.

### Переменные окружения

Скопируйте [`.env.example`](.env.example) в `.env` в корне проекта и задайте нужные значения в локальном файле. Ключи в `.env.example` должны оставаться пустыми. Пример без секретов:

```dotenv
CAREER_QUEST_DATA_DIR=case/case_1/career_quest_dataset
CAREER_QUEST_GROWTH_BUDGET_USD=5.00
CAREER_QUEST_GROWTH_REQUEST_USD=0.10
# CAREER_QUEST_GROWTH_DB=.career-quest/growth.sqlite3
OPENAI_API_KEY=
CAREER_QUEST_AI_PROVIDER=none
CAREER_QUEST_AI_MODEL=
CAREER_QUEST_AI_TIMEOUT=8
NVIDIA_API_KEY=
```

Локальный `.env` исключён из Git; не добавляйте его или ключи в отслеживаемые файлы. Настройки также можно задать в окружении сервера: они имеют приоритет над `.env`. После изменения `.env` или переменных окружения полностью перезапустите сервер Streamlit. Пример ввода ключа в PowerShell без записи значения в историю команд:

```powershell
$apiSecret = Read-Host 'OpenAI API key' -AsSecureString
$env:OPENAI_API_KEY = [System.Net.NetworkCredential]::new('', $apiSecret).Password
.\.venv\Scripts\python.exe -m streamlit run app.py --server.port 8502
```

`CAREER_QUEST_AI_PROVIDER`, `CAREER_QUEST_AI_MODEL`, `CAREER_QUEST_AI_TIMEOUT` и `NVIDIA_API_KEY` сохранены для старого API первой модели. Новый интерфейс использует управляемый AI-подбор из `growth_ai.py`; старый AI-переранжировщик при отображении страниц не запускается. Для нового поиска достаточно `OPENAI_API_KEY`, модель фиксирована для предсказуемого расчёта цены.

**Локальная загрузка ключа из `.env` проверена; живой запрос OpenAI отдельно не проверялся.** Успех, ссылки на источники, кеш, ошибки и таймауты проверены с подставным ответом API. Это не подтверждает доступность модели/инструмента на аккаунте команды или качество реальных рекомендаций.

## Архитектура

```text
Streamlit / ui/growth_views.py
    → ui/core_adapter.py
        → core/growth.py     # HR, SQLite, XP, подтверждённое портфолио
            → core/engine.py    # исходные требования, навыки и объяснения
            → core/growth_ai.py # GPT, поиск, валидация, кеш, журнал бюджета
        → core/api.py        # исходные сценарии кита, импорт, HR-обзор
```

UI не вызывает модель и не ранжирует рекомендации. Он отображает ответы backend и передаёт команды через адаптер. Исходные `core/api.py`, `core/engine.py`, `core/ai.py`, `core/demo.py` и бизнес-проверки сохранены; новые функции реализованы в дополнительных модулях `core/growth*.py`. UI-тесты обновлены для входа и смены ролей через новую страницу. При интеграции входа файлы `core/` не изменялись.

| Файл | Назначение |
|---|---|
| app.py | Демовход, разделы сотрудника, HR и администратора |
| auth/demo_ui.py, auth/demo_session.py | Общая страница входа и выбор роли без базы аккаунтов |
| auth_app.py, auth/service.py, auth/ui.py | Отдельный прототип настоящей регистрации и входа; не используется демовходом |
| ui/core_adapter.py, ui/adapter.py | Интеграция и совместимый фасад |
| ui/growth_views.py | Диаграмма, XP, сертификаты, треки и решения HR |
| ui/components.py, styles/main.css | Представление и читаемая светлая тема |
| ui/development_map.py | Карта навыков и активностей исходного core |
| core/growth.py, core/growth_ai.py | Новые сценарии и ограниченный AI-поиск |
| CORE_NOTES.md | Исходный контракт и формулы первой модели |
| ui/checks/ | Контракт, интеграция, новые HR-сценарии и браузерные проверки |
| tests/ | Исходные тесты core и сквозного сценария |
| ui/demo_adapter.py | Явная заглушка UI-тестов; обычный запуск её не использует |

### Исходный маршрут и карта развития

«Мой маршрут», «Карта развития», «Навыки и требования» и «История участия» сохраняют сценарий стартового кита. Движок рассчитывает разрывы следующего грейда текущей роли, воспроизводит историю после `last_review_date`, проверяет prerequisites и ограничивает gain/max_level. Персональная причина, факторы и ожидаемый прирост показываются отдельно; формулы находятся в `CORE_NOTES.md`.

Кнопка завершения здесь — учебная симуляция кита, явно обозначенная в UI. Она меняет сессионный dataset и прогресс маршрута. Новые XP за обучение начисляются только через одобрение сертификата HR. Игровой уровень опыта и должностной грейд — разные показатели; автоматического повышения нет.

Карта показывает до пяти навыков цели с возможностью раскрыть остальные. Сплошные линии соответствуют `develops_skills`, пунктирные — `prerequisites`. Уровни и прогноз приходят из core; UI не создаёт зависимости или начисления. Графические узлы выбираются через списки над картой. Для Lead показываются требования текущего профиля.

## Импорт проверочного профиля

Войти как **«Администратор»**, открыть **«Импорт данных»**. Можно загрузить профиль JSON, историю CSV или оба файла вместе. Кнопка «Скачать пример профиля» формирует профиль с новым ID `JURY_001`. После импорта нажать «Выйти / сменить роль», выбрать **«Сотрудник»** и новый профиль, затем войти. Импорт остаётся доступен при смене роли в той же вкладке.

- `employees.json`: объект `{"employees": [...]}` или массив профилей. Поля: `employee_id`, `full_name`, `department`, `role`, `grade`, `manager_id`, `hire_date`, `tenure_months`, `work_format`, `preferred_language`, `career_goal`, `skills`, `last_review_date`. Навыки — словарь ID → целое 0–5.
- `activity_history.csv`: `record_id,employee_id,event_id,date,due_date,status,completion_pct,score,feedback_rating,assigned_by`.
- Статусы: `completed`, `in_progress`, `dropped`, `no_show`, `declined`, `overdue`. UTF-8, до 10 МБ на файл.

Профили и история проверяются совместно, включая ссылки между файлами одного импорта. Существующие ID не перезаписываются; неверный импорт не меняет dataset. Каталоги навыков/событий этой формой не импортируются. Импортированные профили и изменения учебного маршрута живут в сессии. HR-записи хранятся отдельно: после перезапуска дополнительный профиль нужно импортировать снова с тем же ID.

## Проверки

```bash
python -m unittest discover -s tests -v
python -m unittest discover -s ui/checks -p "test_*.py" -v
python -m unittest discover -s auth/checks -p "test_*.py" -v
```

AI-тесты подставляют ответы API; платных вызовов не делают. Проверяются 200 профилей кита, прирост и ограничения навыков, импорт и откат ошибок, XP по дням, сохранение оценок, одобрение/отказ сертификата, защита от повторного начисления, запрос курса и решение HR, кеш, источники, таймаут и одновременное резервирование бюджета. Streamlit AppTest проходит новые сценарии целиком с отдельной временной БД.

Браузерные проверки на запущенном сервере:

```bash
python -m pip install playwright==1.63.0
python -m playwright install chromium
python -m ui.checks.browser_check --url http://localhost:8502
python -m ui.checks.theme_check --url http://localhost:8502
python -m ui.checks.growth_browser_check
```

Проверяются профиль → рекомендации → завершение (E0001: 54,5% → 60,6%) → HR → импорт проверочного профиля; видимость диаграммы, кнопок и контраст текста при системной и сохранённой светлой/тёмной теме. `growth_browser_check` сам запускает отдельный сервер с временной БД: оценки/дата → сертификат → одобрение и рост XP → заявка на курс → отказ HR → комментарий сотруднику → сохранение после перезагрузки. Только ответ AI подставной; реальные страницы и действия проходят в Chromium. Скриншоты сохраняются во временную папку из вывода команды. Тесты не заменяют проверку живого OpenAI.

### Graphify

Установленный Graphify используется для карты связей проекта, документации, адаптера и core. Результаты в `graphify-out/`: `graph.json`, `graph.html`, `GRAPH_REPORT.md`, `GRAPH_HEALTH.md`. Это средство анализа кода; рекомендации сотрудникам рассчитывает backend. Ограничения извлечения описаны в отчётах.

## Оставшиеся ограничения

- Страница входа `app.py` предлагает демонстрационные роли сотрудника, HR и администратора без проверки личности. Самостоятельная регистрация из `auth_app.py` пока не связана с рабочими разделами; перед использованием реальными сотрудниками нужны аутентификация и серверная проверка прав на операции.
- Оценки и решения сохраняются, но полной истории редактирования HR-профиля нет. Доказательство сертификата — ссылка или номер, без загрузки/автоматической проверки документа.
- Один курс можно заявить и зачесть один раз. Повторная подача отклонённой заявки и пересмотр окончательного решения пока не реализованы.
- Исходный маршрут/HR-обзор используют сессионные данные кита. Подтверждённый внешний багаж, XP и AI-треки находятся в новых вкладках; HR-обзор кита не объединён с аналитикой внешних курсов.
- Эвристики core не откалиброваны на скрытых профилях жюри. Проценты показывают покрытие требований, не вероятность повышения. История кита использует дату сессии; учебная кнопка симулирует выполнение в дату среза.
- Live API и качество ветвления рекомендаций ещё требуют проверки с ключом команды. При недоступном AI продолжают работать детерминированный маршрут и HR-процессы.
